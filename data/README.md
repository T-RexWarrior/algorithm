# 本地数据

原实验从用户桌面下的“实验三/实验四”等目录读取站点 CSV，数据本身不在原项目内。本目录仅作为统一挂载位置，真实大型数据默认被 `.gitignore` 忽略。

建议按 `raw/`、`interim/`、`processed/` 分层，并在配置中引用相对路径或外部数据根目录。

当前真实数据位置、表头结构和清单更新方式见 `REAL_DATA.md`；逐文件轻量清单见 `real_data_manifest.json`。
