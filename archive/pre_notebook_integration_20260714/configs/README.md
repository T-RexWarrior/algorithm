# 配置目录

当前后期 Notebook 已经使用 `config` 字典，但参数仍保存在 Notebook 内。本目录用于下一阶段把参数外置为 JSON/YAML。

建议每个配置至少记录：数据集标识、训练/验证/测试站点清单、特征列、目标列、时间列、窗口长度、缺失值规则、缩放方法、模型参数、随机种子、损失函数和输出实验名。

本机绝对数据路径、服务器地址和凭据不要写入可提交配置；通过命令行参数或环境变量传入。
